export function maskSecret(_value: string): string {
  // Implementation to be added later
  return '***MASKED***';
}
export function maskReport(_detections: any[]): any {
  return { masked: true };
}
